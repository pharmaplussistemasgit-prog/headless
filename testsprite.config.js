const config = {
    host: 'http://localhost:3000',
    testDir: './tests', // Default test directory
    browsers: ['chromium'], // Default browser
    // Basic Next.js project configuration
};

module.exports = config;
